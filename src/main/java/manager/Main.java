package manager;

import javafx.application.Application;
import manager.presenter.Menu;

public class Main {
    public static void main(String[] arg) {
        Application.launch(Menu.class, arg);
    }
}